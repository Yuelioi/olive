export const event_preprocessor = (message: any, ...args: any) => {
    // console.log(message)
    // console.log(args)
}

export const event_postprocessor = (message: any) => {
    // console.log(message)
}
